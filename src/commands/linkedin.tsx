import {userData} from '../config/user-data'
import {createUrlCommand} from '../utils/create-url-command'

export const linkedinCommand = createUrlCommand('linkedin', userData.linkedin, 'LinkedIn profile')
